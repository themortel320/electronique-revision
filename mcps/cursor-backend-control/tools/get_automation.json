{
  "name": "get_automation",
  "description": "Fetch one Cursor automation by automationId. Stored author text is redacted and workflow values are returned only as a redacted shape before entering chat context.",
  "arguments": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "automationId"
    ],
    "properties": {
      "automationId": {
        "type": "string",
        "minLength": 1,
        "description": "Automation ID (UUID) of the automation to fetch."
      }
    }
  }
}